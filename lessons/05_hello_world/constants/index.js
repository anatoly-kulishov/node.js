const path = require('path');

const FAVICON_URL = '/favicon.ico'
const GET_METHOD = 'GET'
const FAVICON_PATH = path.join(__dirname, 'public', 'favicon.ico');

module.exports = {FAVICON_URL, GET_METHOD, FAVICON_PATH}